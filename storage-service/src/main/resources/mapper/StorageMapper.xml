<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lin.storage.mapper.StorageMapper">

    <resultMap id="BaseResultMap" type="com.lin.storage.entity.Storage">
            <id property="id" column="id" jdbcType="BIGINT"/>
            <result property="productId" column="product_id" jdbcType="BIGINT"/>
            <result property="total" column="total" jdbcType="INTEGER"/>
            <result property="used" column="used" jdbcType="INTEGER"/>
            <result property="residue" column="residue" jdbcType="INTEGER"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,product_id,total,
        used,residue
    </sql>


    <update id="decreaseStorageByProductId">
        update t_storage
        set used = used + #{count},
        residue = residue - #{count}
        where product_id = #{productId} and residue >= #{count}
    </update>

    <!-- ================== TCC 核心 SQL ================== -->
    <!-- TCC Try: 扣除可用库存，增加冻结库存 -->
    <update id="updateTccTry">
        UPDATE t_storage 
        SET residue = residue - #{count}, frozen = frozen + #{count} 
        WHERE product_id = #{productId} AND residue >= #{count}
    </update>

    <!-- TCC Confirm: 扣除冻结库存，增加已用库存 -->
    <update id="updateTccConfirm">
        UPDATE t_storage 
        SET frozen = frozen - #{count}, used = used + #{count} 
        WHERE product_id = #{productId} AND frozen >= #{count}
    </update>

    <!-- TCC Cancel: 扣除冻结库存，恢复可用库存 -->
    <update id="updateTccCancel">
        UPDATE t_storage 
        SET frozen = frozen - #{count}, residue = residue + #{count} 
        WHERE product_id = #{productId} AND frozen >= #{count}
    </update>
</mapper>
